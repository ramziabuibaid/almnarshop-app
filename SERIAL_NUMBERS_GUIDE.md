# دليل نظام الأرقام التسلسلية الاحترافي

## 📋 نظرة عامة

تم تطبيق نظام احترافي لتتبع الأرقام التسلسلية يستخدم **جدول منفصل** لكل سيريال (كما في البرامج الاحترافية مثل SAP و Oracle).

## ✅ المميزات

### 1. جدول منفصل للسيريالات
- كل سيريال له صف منفصل في جدول `serial_numbers`
- يسمح بالبحث والتتبع السريع
- يدعم حالات متعددة (مباع، مرتجع، ضمان، تالف)

### 2. إدخال متعدد للسيريالات
- عند إضافة منتج بكمية 3، تظهر 3 حقول منفصلة
- كل حقل للسيريال خاص بقطعة واحدة
- يتم حفظ كل سيريال في صف منفصل

### 3. البحث والتتبع
- صفحة مخصصة للبحث: `/admin/serial-numbers`
- البحث عن سيريال معين
- عرض تفاصيل كاملة:
  - المنتج
  - نوع الفاتورة ورقمها
  - الزبون
  - تاريخ البيع
  - الحالة

## 🗄️ هيكل قاعدة البيانات

### جدول serial_numbers
```sql
serial_numbers
├── serial_id (UUID) - معرف فريد
├── serial_no (TEXT) - الرقم التسلسلي
├── product_id - المنتج
├── invoice_type - نوع الفاتورة (cash, shop_sales, warehouse_sales, quotation)
├── invoice_id - رقم الفاتورة
├── cash_invoice_detail_id - رابط لتفاصيل الفاتورة النقدية
├── shop_sales_detail_id - رابط لتفاصيل فاتورة المحل
├── warehouse_sales_detail_id - رابط لتفاصيل فاتورة المخزن
├── quotation_detail_id - رابط لتفاصيل عرض السعر
├── customer_id - الزبون
├── sale_date - تاريخ البيع
├── status - الحالة (sold, returned, warranty, damaged)
└── notes - ملاحظات
```

### جداول التفاصيل (للتوافق)
- `serial_no JSONB` - محفوظ أيضاً في جدول التفاصيل (للتوافق مع البيانات القديمة)

## 🚀 خطوات التشغيل

### 1. تشغيل Migrations
```sql
-- في Supabase SQL Editor:

-- أولاً: تشغيل add_serial_number_support.sql
-- ثانياً: تشغيل add_serial_numbers_table.sql
```

### 2. استخدام النظام

#### إدخال السيريالات:
1. افتح أي فاتورة (محل، مخزن، نقدية، أو عرض سعر)
2. أضف منتج بكمية (مثلاً 3)
3. ستظهر 3 حقول منفصلة للسيريال
4. أدخل سيريال مختلف لكل قطعة

#### البحث عن سيريال:
1. اذهب إلى `/admin/serial-numbers`
2. أدخل الرقم التسلسلي
3. اضغط "بحث"
4. اضغط "عرض التفاصيل" لرؤية جميع المعلومات

#### تتبع القطعة:
- يمكنك معرفة:
  - متى تم بيعها
  - لأي زبون
  - في أي فاتورة
  - حالة القطعة حالياً

## 📊 الفرق بين الطريقتين

### ❌ الطريقة القديمة (غير احترافية):
- حفظ array في نفس الصف
- صعوبة في البحث
- لا يمكن تتبع كل قطعة بشكل منفصل

### ✅ الطريقة الجديدة (احترافية):
- جدول منفصل لكل سيريال
- بحث سريع وفعال
- تتبع كامل لكل قطعة
- يدعم حالات متعددة (مباع، مرتجع، ضمان)

## 🔍 أمثلة الاستخدام

### مثال 1: بيع 3 غسالات
```
المنتج: غسالة سامسونج
الكمية: 3
السيريالات:
  - SN001234
  - SN001235
  - SN001236
```

**النتيجة في قاعدة البيانات:**
- 3 صفوف في جدول `serial_numbers`
- كل صف يحتوي على سيريال واحد
- جميعها مرتبطة بنفس الفاتورة والمنتج

### مثال 2: البحث عن سيريال
```
البحث: SN001234
النتائج:
  - المنتج: غسالة سامسونج
  - الفاتورة: SHOP-0001
  - الزبون: أحمد محمد
  - تاريخ البيع: 25/01/2026
  - الحالة: مباع
```

## 📝 ملاحظات مهمة

1. **التوافق مع البيانات القديمة**: السيريالات محفوظة في جدول التفاصيل أيضاً (JSONB)
2. **الحفظ المزدوج**: يتم الحفظ في كلا المكانين للتأكد من عدم فقدان البيانات
3. **البحث**: يستخدم جدول `serial_numbers` للبحث السريع
4. **التتبع**: يمكن تتبع كل قطعة من البيع إلى التسليم

## 🎯 الخطوات التالية (اختيارية)

1. **Migration للبيانات القديمة**: إذا كان لديك بيانات قديمة، يمكن إنشاء script لتحويلها
2. **تحديث الحالة**: إضافة وظائف لتحديث حالة السيريال (مرتجع، ضمان، إلخ)
3. **التقارير**: إضافة تقارير عن السيريالات المباعة
