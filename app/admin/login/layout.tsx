import { Metadata } from "next";

export const metadata: Metadata = {
  title: "تسجيل دخول المدير - Admin Login",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
