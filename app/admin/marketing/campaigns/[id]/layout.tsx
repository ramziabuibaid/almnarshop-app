import { Metadata } from "next";

export const metadata: Metadata = {
  title: "تعديل عرض ترويجي - Edit Campaign",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
